# Project previews

Drop your GIFs here with these exact filenames:

- `tonic.gif`    — preview for cogFlux (tonic/)
- `bug_web.gif`  — preview for Spiking Bug (bug_web/)
- `spike.gif`    — preview for Spike (spike/)

Recommended aspect ratio: 16:10. Recommended size: 640×400 or larger.

While the GIFs are missing, the page falls back to the SVG placeholders
(`tonic.svg`, `bug_web.svg`, `spike.svg`) automatically — no HTML change
needed. As soon as a `.gif` with the matching name appears, it replaces
the placeholder.
